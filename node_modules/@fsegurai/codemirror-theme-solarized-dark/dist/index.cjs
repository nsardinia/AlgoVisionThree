'use strict';

var view = require('@codemirror/view');
var language = require('@codemirror/language');
var highlight = require('@lezer/highlight');

// Helper module for styling options
const generalContent = {
    fontSize: '14px',
    fontFamily: 'JetBrains Mono, Consolas, monospace',
    lineHeight: '1.6',
};
const generalCursor = {
    borderLeftWidth: '2px',
};
const generalDiff = {
    insertedTextDecoration: 'none',
    deletedTextDecoration: 'line-through',
    insertedLinePadding: '1px 3px',
    borderRadious: '3px'};
const generalGutter = {
    border: 'none',
    paddingRight: '8px',
    fontSize: '0.9em',
    fontWeight: '500',
};
const generalPanel = {
    border: 'none',
    borderRadius: '4px',
    padding: '2px 10px',
};
const generalLine = {
    borderRadius: '2px',
};
const generalMatching = {
    borderRadius: '2px',
};
const generalPlaceholder = {
    borderRadius: '4px',
    padding: '0 5px',
    margin: '0 2px',
};
const generalScroller = {
    width: '12px',
    height: '12px',
    borderRadius: '6px',
};
const generalSearchField = {
    borderRadius: '4px',
    padding: '2px 6px',
};
const generalTooltip = {
    borderRadius: '4px',
    borderRadiusSelected: '3px',
    lineHeight: '1.3',
    padding: '4px 8px',
    paddingRight: '8px',
};
/**
 * Function to apply merge revert styles for a theme
 * @param styles Styles for the merge revert buttons
 * @param styles.backgroundColor Background color of the revert area
 * @param styles.borderColor Border color of the revert area
 * @param styles.buttonColor Color of the revert buttons
 * @param styles.buttonHoverColor Hover color of the revert buttons
 */
function applyMergeRevertStyles(styles) {
    // Create a stylesheet
    const styleEl = document.createElement('style');
    styleEl.id = 'cm-merge-revert-styles';
    // Define CSS with the theme-specific values
    styleEl.textContent = `
    .cm-merge-revert {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      padding: 4px;
      background-color: ${styles.backgroundColor};
      border-left: 1px solid ${styles.borderColor};
      border-right: 1px solid ${styles.borderColor};
      width: 32px;
    }
    
    .cm-merge-revert button {
      width: 100%;
      height: auto;
      background-color: transparent;
      border: none;
      color: ${styles.buttonColor};
      cursor: pointer;
      margin: 0 auto;
      font-size: 20px;
    }
    
    .cm-merge-revert button:hover {
      background-color: ${styles.buttonHoverColor};
    }
  `;
    // Remove any existing merge styles
    const existingStyle = document.getElementById('cm-merge-revert-styles');
    if (existingStyle)
        existingStyle.remove();
    // Add the new styles
    document.head.appendChild(styleEl);
}

/**
 * Enhanced Solarized Dark theme color definitions
 * ---------------------------------------------
 * Colors organized by function with visual color blocks
 */
// Base colors from Solarized palette
const base00 = '#002b36', // Background - dark blue
base01 = '#073642', // Lighter background (popups, statuslines)
base02 = '#586e75', // Selection background
base03 = '#657b83', // Comments, invisibles
base04 = '#839496', // Body text
base05 = '#93a1a1', // Default foreground
base06 = '#eee8d5', // Light foreground
base07 = '#fdf6e3', // Light background
// Accent colors from Solarized palette
base08 = '#dc322f', // Red
base09 = '#cb4b16', // Orange
base0A = '#b58900', // Yellow
base0B = '#859900', // Green
base0C = '#2aa198', // Cyan
base0D = '#268bd2', // Blue
base0E = '#6c71c4', // Violet
base0F = '#d33682'; // Magenta
// UI specific colors
const invalid = '#d30102', // Bright red for errors
darkBackground = '#00252f', // Darker background for panels
highlightBackground = '#99eeff0f', // Active line highlight
background = base00, // Main editor background
tooltipBackground = base01, // Tooltip background
selection = '#02B8FF3F', // Selection background with opacity
selectionMatch = '#586e7580', // Selection match with opacity
cursor = base04, // Cursor color
activeBracketBg = '#586e7540', // Active bracket background with opacity
activeBracketBorder = base0D, // Active bracket border - blue
diagnosticWarning = base0A, // Warning color - yellow
linkColor = base0D; // Link color - blue
// Diff/merge specific colors
const addedBackground = '#2aa19820', // Solarized cyan with transparency for insertions
removedBackground = '#dc322f20', // Solarized red with transparency for deletions
addedText = '#859900', // Solarized green for added text
removedText = '#dc322f'; // Solarized red for removed text
/**
 * Enhanced editor theme styles for Solarized Dark
 */
const solarizedDarkTheme = view.EditorView.theme({
    // Base editor styles
    '&': {
        color: base05,
        backgroundColor: background,
        fontSize: generalContent.fontSize,
        fontFamily: generalContent.fontFamily,
    },
    // Content and cursor
    '.cm-content': {
        caretColor: cursor,
        lineHeight: generalContent.lineHeight,
    },
    '.cm-cursor, .cm-dropCursor': {
        borderLeftColor: cursor,
        borderLeftWidth: generalCursor.borderLeftWidth,
    },
    '.cm-fat-cursor': {
        backgroundColor: `${cursor}99`,
        color: background,
    },
    // Selection
    '&.cm-focused > .cm-scroller > .cm-selectionLayer .cm-selectionBackground, .cm-selectionBackground, .cm-content ::selection': {
        backgroundColor: selection,
    },
    // Make sure selection appears above active line
    '.cm-selectionLayer': {
        zIndex: 100,
    },
    // Search functionality
    '.cm-searchMatch': {
        backgroundColor: '#586e7599',
        outline: `1px solid ${base0D}`,
        color: base06,
        borderRadius: generalSearchField.borderRadius,
        '& span': {
            color: base06,
        },
    },
    '.cm-searchMatch.cm-searchMatch-selected': {
        backgroundColor: base0D,
        color: base07,
        padding: generalSearchField.padding,
        '& span': {
            color: base07,
        },
    },
    '.cm-search.cm-panel.cm-textfield': {
        color: base05,
        borderRadius: generalSearchField.borderRadius,
        padding: generalSearchField.padding,
    },
    // Panels
    '.cm-panels': {
        backgroundColor: darkBackground,
        color: base04,
        borderRadius: '4px',
    },
    '.cm-panels.cm-panels-top': {
        borderBottom: `1px solid ${base01}`,
    },
    '.cm-panels.cm-panels-bottom': {
        borderTop: `1px solid ${base01}`,
    },
    '.cm-panel button': {
        backgroundColor: tooltipBackground,
        color: base05,
        border: generalPanel.border,
        borderRadius: generalPanel.borderRadius,
        padding: generalPanel.padding,
    },
    '.cm-panel button:hover': {
        backgroundColor: highlightBackground,
    },
    // Line highlighting
    '.cm-activeLine': {
        backgroundColor: highlightBackground,
        borderRadius: generalLine.borderRadius,
        zIndex: 1,
    },
    // Gutters
    '.cm-gutters': {
        backgroundColor: darkBackground,
        color: base02,
        border: generalGutter.border,
        borderRight: `1px solid ${base01}`,
        paddingRight: generalGutter.paddingRight,
    },
    '.cm-activeLineGutter': {
        backgroundColor: base01,
        color: base05,
        fontWeight: generalGutter.fontWeight,
    },
    '.cm-lineNumbers': {
        fontSize: generalGutter.fontSize,
    },
    '.cm-foldGutter': {
        fontSize: generalGutter.fontSize,
    },
    '.cm-foldGutter .cm-gutterElement': {
        color: base02,
        cursor: 'pointer',
    },
    '.cm-foldGutter .cm-gutterElement:hover': {
        color: base04,
    },
    // Diff/Merge View Styles
    // Inserted/Added Content
    '.cm-insertedLine': {
        textDecoration: generalDiff.insertedTextDecoration,
        backgroundColor: addedBackground,
        color: addedText,
        padding: generalDiff.insertedLinePadding,
        borderRadius: generalDiff.borderRadious,
    },
    'ins.cm-insertedLine, ins.cm-insertedLine:not(:has(.cm-changedText))': {
        textDecoration: generalDiff.insertedTextDecoration,
        backgroundColor: `${addedBackground} !important`,
        color: addedText,
        padding: generalDiff.insertedLinePadding,
        borderRadius: generalDiff.borderRadious,
        border: `1px solid ${addedText}40`,
    },
    'ins.cm-insertedLine .cm-changedText': {
        background: 'transparent !important',
    },
    // Deleted/Removed Content
    '.cm-deletedLine': {
        textDecoration: generalDiff.deletedTextDecoration,
        backgroundColor: removedBackground,
        color: removedText,
        padding: generalDiff.insertedLinePadding,
        borderRadius: generalDiff.borderRadious,
    },
    'del.cm-deletedLine, del, del:not(:has(.cm-deletedText))': {
        textDecoration: generalDiff.deletedTextDecoration,
        backgroundColor: `${removedBackground} !important`,
        color: removedText,
        padding: generalDiff.insertedLinePadding,
        borderRadius: generalDiff.borderRadious,
        border: `1px solid ${removedText}40`,
    },
    'del .cm-deletedText, del .cm-changedText': {
        background: 'transparent !important',
    },
    // Tooltips and autocomplete
    '.cm-tooltip': {
        backgroundColor: tooltipBackground,
        border: `1px solid ${base02}`,
        borderRadius: generalTooltip.borderRadius,
        padding: generalTooltip.padding,
        boxShadow: '0 2px 8px rgba(0, 0, 0, 0.3)',
    },
    '.cm-tooltip-autocomplete': {
        '& > ul': {
            backgroundColor: tooltipBackground,
            border: 'none',
        },
        '& > ul > li': {
            padding: generalTooltip.padding,
            lineHeight: generalTooltip.lineHeight,
        },
        '& > ul > li[aria-selected]': {
            backgroundColor: selection,
            color: base06,
            borderRadius: generalTooltip.borderRadiusSelected,
        },
        '& > ul > li > span.cm-completionIcon': {
            color: base03,
            paddingRight: generalTooltip.paddingRight,
        },
        '& > ul > li > span.cm-completionDetail': {
            color: base03,
            fontStyle: 'italic',
        },
    },
    '.cm-tooltip .cm-tooltip-arrow:before': {
        borderTopColor: 'transparent',
        borderBottomColor: 'transparent',
    },
    '.cm-tooltip .cm-tooltip-arrow:after': {
        borderTopColor: tooltipBackground,
        borderBottomColor: tooltipBackground,
    },
    // Diagnostics styling
    '.cm-diagnostic': {
        '&-error': {
            borderLeft: `3px solid ${base08}`,
        },
        '&-warning': {
            borderLeft: `3px solid ${diagnosticWarning}`,
        },
        '&-info': {
            borderLeft: `3px solid ${linkColor}`,
        },
    },
    '.cm-lintPoint-error': {
        borderBottom: `2px wavy ${base08}`,
    },
    '.cm-lintPoint-warning': {
        borderBottom: `2px wavy ${diagnosticWarning}`,
    },
    // Matching brackets
    '.cm-matchingBracket': {
        backgroundColor: activeBracketBg,
        outline: `1px solid ${activeBracketBorder}`,
        borderRadius: generalMatching.borderRadius,
    },
    '.cm-nonmatchingBracket': {
        backgroundColor: `${base08}40`,
        outline: `1px solid ${invalid}`,
        borderRadius: generalMatching.borderRadius,
    },
    // Selection matches
    '.cm-selectionMatch': {
        backgroundColor: selectionMatch,
        outline: `1px solid ${base02}50`,
        borderRadius: generalMatching.borderRadius,
    },
    // Fold placeholder
    '.cm-foldPlaceholder': {
        backgroundColor: tooltipBackground,
        color: base03,
        border: `1px dotted ${base02}70`,
        borderRadius: generalPlaceholder.borderRadius,
        padding: generalPlaceholder.padding,
        margin: generalPlaceholder.margin,
    },
    // Focus outline
    '&.cm-focused': {
        outline: 'none',
        boxShadow: `0 0 0 2px ${background}, 0 0 0 3px ${base0D}40`,
    },
    // Scrollbars
    '& .cm-scroller::-webkit-scrollbar': {
        width: generalScroller.width,
        height: generalScroller.height,
    },
    '& .cm-scroller::-webkit-scrollbar-track': {
        background: darkBackground,
    },
    '& .cm-scroller::-webkit-scrollbar-thumb': {
        backgroundColor: base01,
        borderRadius: generalScroller.borderRadius,
        border: `3px solid ${darkBackground}`,
    },
    '& .cm-scroller::-webkit-scrollbar-thumb:hover': {
        backgroundColor: base02,
    },
    // Ghost text
    '.cm-ghostText': {
        opacity: '0.5',
        color: base03,
    },
}, { dark: true });
/**
 * Enhanced syntax highlighting for Solarized Dark theme
 */
const solarizedDarkHighlightStyle = language.HighlightStyle.define([
    // Keywords and control flow
    { tag: highlight.tags.keyword, color: base0B, fontWeight: 'bold' },
    { tag: highlight.tags.controlKeyword, color: base0B, fontWeight: 'bold' },
    { tag: highlight.tags.moduleKeyword, color: base0B, fontWeight: 'bold' },
    // Names and variables
    { tag: [highlight.tags.name, highlight.tags.deleted, highlight.tags.character, highlight.tags.macroName], color: base0C },
    { tag: [highlight.tags.variableName], color: base05 },
    { tag: [highlight.tags.propertyName], color: base0C, fontStyle: 'normal' },
    // Classes and types
    { tag: [highlight.tags.typeName], color: base09 },
    { tag: [highlight.tags.className], color: base09, fontStyle: 'italic' },
    { tag: [highlight.tags.namespace], color: base0F, fontStyle: 'italic' },
    // Operators and punctuation
    { tag: [highlight.tags.operator, highlight.tags.operatorKeyword], color: base0E },
    { tag: [highlight.tags.bracket], color: base0F },
    { tag: [highlight.tags.brace], color: base0F },
    { tag: [highlight.tags.punctuation], color: base04 },
    // Functions and parameters
    { tag: [highlight.tags.function(highlight.tags.variableName)], color: base0D },
    { tag: [highlight.tags.labelName], color: base0F },
    { tag: [highlight.tags.definition(highlight.tags.function(highlight.tags.variableName))], color: base0D },
    { tag: [highlight.tags.definition(highlight.tags.variableName)], color: base0C },
    // Constants and literals
    { tag: highlight.tags.number, color: base0F },
    { tag: highlight.tags.changed, color: base0F },
    { tag: highlight.tags.annotation, color: invalid, fontStyle: 'italic' },
    { tag: highlight.tags.modifier, color: base0F, fontStyle: 'italic' },
    { tag: highlight.tags.self, color: base0F },
    {
        tag: [highlight.tags.color, highlight.tags.constant(highlight.tags.name), highlight.tags.standard(highlight.tags.name)],
        color: base0A,
    },
    { tag: [highlight.tags.atom, highlight.tags.bool, highlight.tags.special(highlight.tags.variableName)], color: base0F },
    // Strings and regex
    { tag: [highlight.tags.processingInstruction, highlight.tags.inserted], color: base0B },
    { tag: [highlight.tags.special(highlight.tags.string), highlight.tags.regexp], color: invalid },
    { tag: highlight.tags.string, color: base0A },
    // Punctuation and structure
    { tag: highlight.tags.definition(highlight.tags.typeName), color: base09, fontWeight: 'bold' },
    { tag: [highlight.tags.definition(highlight.tags.name), highlight.tags.separator], color: base0C },
    // Comments and documentation
    { tag: highlight.tags.meta, color: base08 },
    { tag: highlight.tags.comment, fontStyle: 'italic', color: base02 },
    { tag: highlight.tags.docComment, fontStyle: 'italic', color: base02 },
    // HTML/XML elements
    { tag: [highlight.tags.tagName], color: base0D },
    { tag: [highlight.tags.attributeName], color: base05 },
    // Markdown and text formatting
    { tag: [highlight.tags.heading], fontWeight: 'bold', color: base0A },
    { tag: highlight.tags.heading1, color: base07 },
    { tag: highlight.tags.heading2, color: base06 },
    { tag: highlight.tags.heading3, color: base06 },
    { tag: highlight.tags.heading4, color: base06 },
    { tag: highlight.tags.heading5, color: base06 },
    { tag: highlight.tags.heading6, color: base06 },
    { tag: [highlight.tags.strong], fontWeight: 'bold', color: base06 },
    { tag: [highlight.tags.emphasis], fontStyle: 'italic', color: base0B },
    // Links and URLs
    {
        tag: [highlight.tags.link],
        color: base0C,
        fontWeight: '500',
        textDecoration: 'underline',
        textUnderlinePosition: 'under',
    },
    {
        tag: [highlight.tags.url],
        color: base0A,
        textDecoration: 'underline',
        textUnderlineOffset: '2px',
    },
    // Special states
    {
        tag: [highlight.tags.invalid],
        color: base02,
        textDecoration: 'underline wavy',
        borderBottom: `1px dotted ${base08}`,
    },
    { tag: [highlight.tags.strikethrough], color: invalid, textDecoration: 'line-through' },
    // Enhanced syntax highlighting
    { tag: highlight.tags.constant(highlight.tags.name), color: base0A },
    { tag: highlight.tags.deleted, color: base08 },
    { tag: highlight.tags.squareBracket, color: base08 },
    { tag: highlight.tags.angleBracket, color: base02 },
    // Additional specific styles
    { tag: highlight.tags.monospace, color: base05 },
    { tag: [highlight.tags.contentSeparator], color: base0A },
    { tag: highlight.tags.quote, color: base0B },
]);
/**
 * Combined Solarized Dark theme extension
 */
const solarizedDark = [
    solarizedDarkTheme,
    language.syntaxHighlighting(solarizedDarkHighlightStyle),
];
/**
 * Solarized Dark merge revert styles configuration
 */
const solarizedDarkMergeStyles = {
    backgroundColor: darkBackground,
    borderColor: base02,
    buttonColor: base05,
    buttonHoverColor: base01,
};

exports.applyMergeRevertStyles = applyMergeRevertStyles;
exports.solarizedDark = solarizedDark;
exports.solarizedDarkMergeStyles = solarizedDarkMergeStyles;
